# Frontend env vars (Vite reads VITE_* at build time)

# Production: set this to your Railway backend URL, e.g. https://three60-dash-production.up.railway.app
# Development: leave unset — Vite proxies /api → http://127.0.0.1:8000
VITE_API_BASE=
